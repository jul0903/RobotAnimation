using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArmManager : MonoBehaviour
{
    //[SerializeField] List<GradientArm> arms;

    //private void Update()
    //{

    //    for (int i = 0; i < arms.Count; i++)
    //    {
    //        foreach(GradientClaw claw in arms[i].claws)
    //        {
    //            if (claw.doNotOpenAgain)
    //            {
    //                arms[i].SetNewtarget();
    //                Debug.Log(arms[i].newTarget.position);
    //            }
    //        }

    //    }
    //}
}
